name: Probe

# Temporary. One job, one step, no actions, no checkout. If this fails the same
# way the real pipeline does, the cause is not the pipeline. Removed as soon as
# the question is answered.

on:
  push:
    branches: ["claude/**"]
  workflow_dispatch:

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo "runner reached"
